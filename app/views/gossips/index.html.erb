<!doctype html>
<html>

<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>

<header>

<b><%= flash[:info] %></b>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/accueil">Accueil</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/city">Ville <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/team">Team <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/contact">Contact</a>
      </li>
      <li class="nav-item">
        <% if current_user %>
          <%= link_to "Log out", sessions_path, method: :delete %>
        <% else %>
          <%= link_to "Sign in ", new_session_path %>
          <%= link_to "Sign up ", new_user_path %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>

</header>

<body style="margin:30px">

<h1> 👋 Helloooo !! 🎉 </h1>
<h2> ✉️🔥 Voici les derniers potins ☕️ : </h2>

<div class="row">


<%= link_to "Create new gossip", new_gossip_path %>

<% @gossips.each do |gossip| %>
 
  <div class="col-sm-6">
    <div class="card" >
      <div class="card-body">
        <a href="/gossips/<%= gossip.id %>" class="card-title">
          <%= gossip.city.name %> :
          <%= gossip.title %>
        </a>
      </div>
    </div>
  </div>

<% end %>

</div>

</body>

</html>



